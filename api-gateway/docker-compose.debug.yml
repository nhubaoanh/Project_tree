version: '3.4'

services:
  gateway:
    image: gateway
    build:
      context: .
      dockerfile: ./Dockerfile
    environment:
      NODE_ENV: development
    ports:
      - 6688:6688
      - 9229:9229
    command: ["node", "--inspect=0.0.0.0:9229", "server.js"]
